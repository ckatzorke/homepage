language: node_js
node_js:
  - "8"
cache:
  directories:
    - "node_modules"

env:
  - CI=true

deploy:
  provider: script
  script: netlify deploy -s $NETLIFY_SITEID -t $NETLIFY_TOKEN -p ./public
  skip_cleanup: true
  on:
    branch: master
